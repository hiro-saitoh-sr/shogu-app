# shogu-app PROJECT STATUS

## 正本
- ソースコード: GitHubリポジトリ `hiro-saitoh-sr/shogu-app`
- 本番公開先: GitHub Pages
- 連携データ: task-appと共通のFirebase Realtime Database

## Git状態
- 対象ブランチ: `main`
- 直近コミット: 編集ポップアップにステータスを追加（2026-08-28）
- 2026-08-28作業開始時点で `origin/main` と同期済み（ahead 0 / behind 0）

## 現在の実装状況
- 処遇改善加算、ベースアップ評価料、臨時の補助金の計画書・実績報告書を管理
- ステータス管理、提出期日一括設定、顧問先マスタ管理を実装
- Firebase Authenticationの許可対象は業務用アカウント2件（Firebase側で管理）
- CodexとClaude Codeは対等な開発担当であり、共通Git手順と競合停止ルールを適用する

## Firebaseパス
### 実装上確認済み
- `shogu/masters`
- `shogu/deliveries/shoguPlan`
- `shogu/deliveries/shoguReport`
- `shogu/deliveries/rinjiPlan`
- `shogu/deliveries/rinjiReport`
- `shogu/deliveries/baseupPlan`
- `shogu/deliveries/baseupInterim`
- `shogu/deliveries/baseupReport`
- 各deliveries配下の `status`
- `shogu/rinjiSubsidyNames/{年度}`（臨時の補助金の年度ごとの補助金名称、値は文字列。2026-08-21追加）

### 実データ未確認
- 上記パスは `index.html` の参照・書込実装から確認したもので、Firebase上の実データの存在・内容・件数は未確認
- 旧文書にあった `shogu/customers` と `shogu/cases` は現行実装から確認できず、実データも未確認

## 直近の変更
- 編集ポップアップ（`DeliveryModal`）に「ステータス」欄を追加（2026-08-28）。
  - 表示位置：顧問先名の下、提出先リストの上。選択肢は既存の`STATUS_LIST`（対象あり・依頼なし・依頼受付・資料待ち・収集済・集計完了・納品済）の7段階。
  - 対象は処遇改善加算・ベースアップ評価料の編集ポップアップのみ。臨時の補助金（`isRinji`）は区分（行）ごとに個別のステータスを持つため対象外とし、欄自体を非表示。
  - データの持ち方は既存のグループ単位`statusMap`（`shogu/deliveries/{path}/status/{gid}`）をそのまま利用。新しいFirebaseパスは作らず、データ移行も不要。
  - 保存方法：`DeliveryModal`の保存ボタンで`onSave(group, newStatus)`のように新ステータスを渡し、`DeliveryView.handleGroupSave`で既存`statusMap`にマージしてから`App.handleDeliverySave(rows, statusData)`に渡し、行データとステータスを1回の`saveDeliveryRows`呼び出し（Firebaseへの1回の`.set()`）で同時に書き込む方式にした。一覧画面のステータスバッジのクリック変更（`onStatusChange`、`shogu/deliveries/{path}/status/{gid}`への個別書込み）とは別経路だが、同じ`shogu/deliveries/{path}/status`ノードを参照するため一覧・編集ポップアップ間のステータスは常に同期する。
  - 新規登録（追加モード）時の初期値は`'対象あり'`。既存グループを編集する場合は現在の`statusMap`の値（未設定時は`'対象あり'`）を初期表示。
  - 既存動作への影響：ステータス欄追加以外の項目（顧問先名・提出先リスト等）のロジックは変更なし。臨時の補助金タブの編集ポップアップ・区分ごとステータスの動作にも影響なし。
- 全タブのステータスを統一・編集ポップアップの提出担当を削除（2026-08-27）。
  - `STATUS_LIST`/`STATUS_COLORS`を新7段階に変更：対象あり(#E53935)・依頼なし(#757575)・依頼受付(#00ACC1・新規追加)・資料待ち(#8E24AA)・収集済(#039BE5)・集計完了(#43A047)・納品済(#FB8C00)。処遇改善加算・ベースアップ評価料・臨時の補助金（区分ごと）すべてに反映。絞り込みフィルターは`STATUS_LIST`参照のため自動反映。
  - 既存データの移行方式：`shogu/deliveries/*`から値を読み込む全箇所（`flatToGroups`のitem.status、7本の`status`ノードonValueリスナー）に`normalizeStatus`/`normalizeStatusMap`（`LEGACY_STATUS_MAP = {未着手→対象あり, 依頼中→資料待ち, 自社作成→依頼なし}`）を適用し、読み込み時に自動変換。既存の`normalizeTeishutsu`（提出担当の旧名称吸収）と同じ方式。Firebase上の生データ文字列は書き換えない（利用者承認済み・2026-08-27）。「依頼受付」は今回新規追加のステータスで既存データには存在しないため移行対象なし。
  - 計画書からコピー・前年度からコピー実行時の新規グループ/行のステータスを`'対象あり'`にリセット（`makeCopiedGroups`）。
  - 編集ポップアップ（`DeliveryModal`）から「提出担当」ドロップダウンを削除。データフィールド自体（`提出担当`、`TEISHUTSU_OPTIONS`は削除済み定数）は残し、新規登録時デフォルト`'自社'`のまま非表示で保持。既存データの`提出担当`値・`isRowComplete`判定への影響なし。
- 臨時の補助金の補助金名称の表示位置・編集欄の幅を修正（2026-08-22）。
  - 原因①：サブタブ行のコンテナに`justifyContent:'space-between'`を指定していたため、`RinjiSubsidyNameEditor`が画面右端まで押し出されていた。`justifyContent`を外し、タブボタン群のすぐ右（`gap:'16px'`）に配置するよう変更。
  - 原因②：編集用`<input>`に`minWidth:'280px'`のみ指定しており、アプリ全体のグローバルCSS（`input, select, textarea { width:100% }`）により、編集モード時に親要素（サブタブ行）いっぱいに広がっていた。`minWidth`を`width:'240px'`（+`flex:'none'`）に変更し、幅を固定。
  - 表示・保存ロジック（Firebaseパス`shogu/rinjiSubsidyNames/{年度}`、クリック編集、Enter/フォーカスアウト確定）自体には変更なし。
- 臨時の補助金タブの「計画書」「実績報告」サブタブ行の右側に、年度ごとの補助金名称を表示・クリック編集できる機能を追加（2026-08-21）。
  - 新規コンポーネント `RinjiSubsidyNameEditor`（未入力時は「補助金名称を入力...」をプレースホルダー表示、クリックで入力欄化、Enterまたはフォーカスアウトで確定・保存）。
  - Firebaseパス `shogu/rinjiSubsidyNames/{年度}` に文字列として保存。既存の `shogu/deliveries/*`・`shogu/masters` とは独立した新規パスで、既存データへの影響なし。
  - 表示は `mainTab === '臨時の補助金'` の場合のみ、選択中の年度（`selNendo`）に対応する名称を表示。処遇改善加算・ベースアップ評価料タブには影響なし。
- 臨時の補助金（計画書・実績報告）のステータスを「1顧問先1ステータス」から「区分（行）ごとのステータス」に変更（2026-08-21）。処遇改善加算・ベースアップ評価料は従来通りグループ単位の`statusMap`のまま変更なし。
  - 各行データに既存の未使用フィールド`status`（`DELIVERY_FIELDS`・`flatToGroups`/`groupsToFlat`に元々存在）を利用。新しいFirebaseパスは作らず、データ移行も不要（既存データは`status`未設定のため読み込み時に「未着手」扱い）。
  - 表示ルール：`提出先URL`または`deadlineDate`のいずれかがある行にのみステータスバッジを表示（`itemHasStatus()`）。どちらもない行は空白。
  - クリックで次のステータスへ遷移する処理は既存の`onItemFieldChange`（`handleDeliveryItemFieldChange`）をそのまま利用（グループid＋保険者idで行を特定し`{status: ...}`を保存）。
  - 絞り込みフィルターは、臨時の補助金タブのみ「いずれかの区分（表示対象の行）が該当ステータスであれば顧問先全体を表示」に変更。処遇改善加算・ベースアップ評価料は従来通り`statusMap`基準のまま。
- 全タブ共通のステータス選択肢（`STATUS_LIST`/`STATUS_COLORS`）に「自社作成」を追加（2026-08-21）。処遇改善加算・ベースアップ評価料・臨時の補助金すべてのステータスバッジのクリック順・絞り込みフィルターの選択肢に反映。
- 臨時の補助金・実績報告ページのボタンを変更（「処遇改善加算からコピー」「ベースアップ評価料からコピー」を削除し、既存の「計画書からコピー」と同一動作のボタンを追加）。臨時の補助金・計画書ページのボタンは変更なし。
- 全タブ共通の絞り込みフィルターから「年度（すべて）」を削除し、「納品日（すべて）」（入力済み/未入力）を「ステータス（すべて）」（未着手/依頼中/収集済/集計完了/納品済/自社作成）に変更
- 対象は `DeliveryView` コンポーネント（処遇改善加算・ベースアップ評価料・臨時の補助金の全タブで共通利用）

## デプロイ・公開状況
- 公開URL: https://hiro-saitoh-sr.github.io/shogu-app/
- 今回は `index.html` のボタン・絞り込みフィルターのみ変更し、Firebase・GAS・データ構造は変更しない

## 検証結果
- 2026-08-28の編集ポップアップへのステータス追加はesbuild（jsxトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施（過去の変更と同様の状況）。
- 2026-08-27の変更もesbuild（jsxトランスフォーム）で構文検証済み。ログイン用の業務用アカウントを保有していないためChrome拡張機能での実ブラウザ動作確認は未実施。次回ログイン時に「既知の課題」を確認する必要あり
- `git fetch origin` 後、作業開始時にローカルと `origin/main` の一致を確認
- Babel（アプリ実行時と同じJSXトランスパイラ）で構文検証済み
- Chrome拡張機能が未接続のためブラウザでの実動作確認は未実施（利用者の承認を得て実施）
- 2026-08-21の変更もesbuild（jsxトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 2026-08-21の補助金名称機能もesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認なしでpush
- 2026-08-22の表示位置・編集欄幅の修正もesbuildで構文検証済み。低リスクな表示調整のため実ブラウザでの動作確認なしでpush

## 既知の課題
- 2026-08-28の変更（編集ポップアップにステータス追加）について実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 処遇改善加算・ベースアップ評価料の編集ポップアップに「ステータス」欄が顧問先名の下・提出先リストの上に表示されること
  - 選択肢が「対象あり・依頼なし・依頼受付・資料待ち・収集済・集計完了・納品済」の7段階であること
  - 既存グループを編集で開いたとき、一覧画面のステータスバッジと同じ値が初期選択されていること
  - 編集ポップアップでステータスを変更して保存すると、Firebaseに反映され一覧画面のバッジ表示にも反映されること（一覧のバッジをクリックして変更した場合との相互整合性も確認）
  - 新規追加時は「対象あり」がデフォルトで保存されること
  - 臨時の補助金タブの編集ポップアップには「ステータス」欄が表示されないこと、区分ごとのステータス機能に影響がないこと
- 2026-08-27の変更（ステータス統一・提出担当削除）について実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 全タブのステータスバッジ表示・クリック順が「対象あり→依頼なし→依頼受付→資料待ち→収集済→集計完了→納品済」の7段階になっていること
  - 旧データ（未着手/依頼中/自社作成）が開いた瞬間に新名称（対象あり/資料待ち/依頼なし）で表示されること（Firebase上の生データは書き換えていないため、Firebaseコンソールで直接見ると旧名称のまま）
  - 絞り込みフィルターの選択肢が新7段階になっていること
  - 計画書からコピー・前年度からコピーの新規行が「対象あり」になること
  - 処遇改善加算・ベースアップ評価料・臨時の補助金（区分ごと）すべてで反映されていること
  - 編集ポップアップ（届出を追加／編集）に「提出担当」欄が表示されないこと
  - 提出担当を使う既存の完了判定（`isRowComplete`）に影響がないこと
- Firebaseの実データ構造と件数は未確認
- 今回（2026-08-21）の変更について実ブラウザでの動作確認が未実施。特に以下を次回ログイン時に確認する必要あり：
  - 臨時の補助金タブで区分ごとにステータスバッジが表示され、クリックで個別に遷移すること
  - リンクURL・提出期日がともにない行はステータス列が空白になること
  - 絞り込みフィルターで「いずれかの区分が該当ステータス」の顧問先が表示されること
  - 処遇改善加算・ベースアップ評価料タブの既存動作（1顧問先1ステータス）に影響がないこと
  - 全タブのステータス選択肢・絞り込みフィルターに「自社作成」が追加されていること
- 臨時の補助金の補助金名称機能（2026-08-21追加）も実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 臨時の補助金タブの計画書・実績報告サブタブ行の右側に名称欄が表示されること
  - クリックで編集でき、Enter・フォーカスアウトで保存されること
  - 年度を切り替えると対応する年度の名称に表示が切り替わること
  - 計画書・実績報告で同じ名称が共有されること、再読み込み後も保持されること
  - 処遇改善加算・ベースアップ評価料タブの表示に影響がないこと
- 補助金名称の表示位置・編集欄幅の修正（2026-08-22）も実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 補助金名称がタブのすぐ右隣（画面右端ではなく）に表示されること
  - ✏️クリックで編集モードにした際、入力欄が画面いっぱいに広がらず適切な幅（240px程度）に収まること

## 2026年8月3日 実施内容
- 臨時の補助金 実績報告ページのボタン変更
  - 「処遇改善加算からコピー」「ベースアップ評価料からコピー」を削除
  - 「計画書からコピー」ボタンを追加
- 全タブの絞り込みフィルター修正
  - 「年度（すべて）」フィルターを削除
  - 「納品日（すべて）」→「ステータス（すべて）」に変更
  - ステータス選択肢：すべて・未着手・依頼中・収集済・集計完了・納品済
  - ステータス選択肢のセレクトボックス幅を修正

## NEXT_ACTION
- 次回作業開始時に `git fetch origin`、指示文書、Git状態を再確認する
- Firebase関連の変更時は、実データ確認と影響範囲整理を先に行う
- 次回ログイン時に、2026-08-21の変更（臨時の補助金の区分ごとステータス・全タブ「自社作成」追加・補助金名称表示編集機能）と2026-08-22の表示位置・編集欄幅修正を実ブラウザで確認する（上記「既知の課題」参照）

## 最終更新
- 最終更新AI: Claude Code
- 最終更新日時: 2026-08-28（日本時間）
- 変更内容: 処遇改善加算・ベースアップ評価料の編集ポップアップ（`DeliveryModal`）に「ステータス」欄を追加（顧問先名の下・提出先リストの上）。選択肢は既存の`STATUS_LIST`7段階をそのまま使用し、既存のグループ単位`statusMap`（`shogu/deliveries/{path}/status/{gid}`）に保存。臨時の補助金は区分ごとに個別ステータスを持つため対象外。詳細は「直近の変更」参照。実ブラウザでの動作確認は未実施（Chrome拡張機能未接続のため）。

### 過去の更新

- 2026-08-27（Claude Code）: 全タブ（処遇改善加算・ベースアップ評価料・臨時の補助金）のステータス選択肢を「対象あり・依頼なし・依頼受付・資料待ち・収集済・集計完了・納品済」の7段階に統一。既存データ（未着手/依頼中/自社作成）は読み込み時の自動変換方式（`normalizeStatus`）で新名称表示に対応、Firebase上の生データは書き換えなし（利用者承認済み）。編集ポップアップから「提出担当」欄を削除。実ブラウザでの動作確認は未実施（ログイン用アカウント未保有のため）。最終更新日時: 2026-08-27（日本時間）

- 2026-08-22（Claude Code）: 臨時の補助金の補助金名称について、①サブタブ行の`justifyContent:'space-between'`により画面右端に表示されていたのをタブのすぐ右隣に変更、②編集用入力欄がグローバルCSSの`width:100%`により画面いっぱいに広がっていたのを240px固定幅に変更。最終更新日時: 2026-08-22（日本時間）
- 2026-08-03（Claude Code）: 臨時の補助金・実績報告ページのボタン変更と全タブの絞り込みフィルター修正。詳細は「直近の変更」参照。最終更新日時: 2026-08-03（日本時間）
