# shogu-app PROJECT STATUS

## 正本
- ソースコード: GitHubリポジトリ `hiro-saitoh-sr/shogu-app`
- 本番公開先: GitHub Pages
- 連携データ: task-appと共通のFirebase Realtime Database

## Git状態
- 対象ブランチ: `main`
- 直近コミット: `e47e04e`（臨時の補助金に年度ごとの補助金名称表示・編集機能を追加）
- 2026-08-21確認時点で `origin/main` と同期済み（ahead 0 / behind 0）

## 現在の実装状況
- 処遇改善加算、ベースアップ評価料、臨時の補助金の計画書・実績報告書を管理
- ステータス管理、提出期日一括設定、顧問先マスタ管理を実装
- Firebase Authenticationの許可対象は業務用アカウント2件（Firebase側で管理）
- CodexとClaude Codeは対等な開発担当であり、共通Git手順と競合停止ルールを適用する

## Firebaseパス
### 実装上確認済み
- `shogu/masters`
- `shogu/deliveries/shoguPlan`
- `shogu/deliveries/shoguReport`
- `shogu/deliveries/rinjiPlan`
- `shogu/deliveries/rinjiReport`
- `shogu/deliveries/baseupPlan`
- `shogu/deliveries/baseupInterim`
- `shogu/deliveries/baseupReport`
- 各deliveries配下の `status`
- `shogu/rinjiSubsidyNames/{年度}`（臨時の補助金の年度ごとの補助金名称、値は文字列。2026-08-21追加）

### 実データ未確認
- 上記パスは `index.html` の参照・書込実装から確認したもので、Firebase上の実データの存在・内容・件数は未確認
- 旧文書にあった `shogu/customers` と `shogu/cases` は現行実装から確認できず、実データも未確認

## 直近の変更
- 臨時の補助金タブの「計画書」「実績報告」サブタブ行の右側に、年度ごとの補助金名称を表示・クリック編集できる機能を追加（2026-08-21）。
  - 新規コンポーネント `RinjiSubsidyNameEditor`（未入力時は「補助金名称を入力...」をプレースホルダー表示、クリックで入力欄化、Enterまたはフォーカスアウトで確定・保存）。
  - Firebaseパス `shogu/rinjiSubsidyNames/{年度}` に文字列として保存。既存の `shogu/deliveries/*`・`shogu/masters` とは独立した新規パスで、既存データへの影響なし。
  - 表示は `mainTab === '臨時の補助金'` の場合のみ、選択中の年度（`selNendo`）に対応する名称を表示。処遇改善加算・ベースアップ評価料タブには影響なし。
- 臨時の補助金（計画書・実績報告）のステータスを「1顧問先1ステータス」から「区分（行）ごとのステータス」に変更（2026-08-21）。処遇改善加算・ベースアップ評価料は従来通りグループ単位の`statusMap`のまま変更なし。
  - 各行データに既存の未使用フィールド`status`（`DELIVERY_FIELDS`・`flatToGroups`/`groupsToFlat`に元々存在）を利用。新しいFirebaseパスは作らず、データ移行も不要（既存データは`status`未設定のため読み込み時に「未着手」扱い）。
  - 表示ルール：`提出先URL`または`deadlineDate`のいずれかがある行にのみステータスバッジを表示（`itemHasStatus()`）。どちらもない行は空白。
  - クリックで次のステータスへ遷移する処理は既存の`onItemFieldChange`（`handleDeliveryItemFieldChange`）をそのまま利用（グループid＋保険者idで行を特定し`{status: ...}`を保存）。
  - 絞り込みフィルターは、臨時の補助金タブのみ「いずれかの区分（表示対象の行）が該当ステータスであれば顧問先全体を表示」に変更。処遇改善加算・ベースアップ評価料は従来通り`statusMap`基準のまま。
- 全タブ共通のステータス選択肢（`STATUS_LIST`/`STATUS_COLORS`）に「自社作成」を追加（2026-08-21）。処遇改善加算・ベースアップ評価料・臨時の補助金すべてのステータスバッジのクリック順・絞り込みフィルターの選択肢に反映。
- 臨時の補助金・実績報告ページのボタンを変更（「処遇改善加算からコピー」「ベースアップ評価料からコピー」を削除し、既存の「計画書からコピー」と同一動作のボタンを追加）。臨時の補助金・計画書ページのボタンは変更なし。
- 全タブ共通の絞り込みフィルターから「年度（すべて）」を削除し、「納品日（すべて）」（入力済み/未入力）を「ステータス（すべて）」（未着手/依頼中/収集済/集計完了/納品済/自社作成）に変更
- 対象は `DeliveryView` コンポーネント（処遇改善加算・ベースアップ評価料・臨時の補助金の全タブで共通利用）

## デプロイ・公開状況
- 公開URL: https://hiro-saitoh-sr.github.io/shogu-app/
- 今回は `index.html` のボタン・絞り込みフィルターのみ変更し、Firebase・GAS・データ構造は変更しない

## 検証結果
- `git fetch origin` 後、作業開始時にローカルと `origin/main` の一致を確認
- Babel（アプリ実行時と同じJSXトランスパイラ）で構文検証済み
- Chrome拡張機能が未接続のためブラウザでの実動作確認は未実施（利用者の承認を得て実施）
- 2026-08-21の変更もesbuild（jsxトランスフォーム）で構文検証済み。Chrome拡張機能が未接続のため実ブラウザでの動作確認は未実施
- 2026-08-21の補助金名称機能もesbuildで構文検証済み。利用者の判断により実ブラウザでの動作確認なしでpush

## 既知の課題
- Firebaseの実データ構造と件数は未確認
- 今回（2026-08-21）の変更について実ブラウザでの動作確認が未実施。特に以下を次回ログイン時に確認する必要あり：
  - 臨時の補助金タブで区分ごとにステータスバッジが表示され、クリックで個別に遷移すること
  - リンクURL・提出期日がともにない行はステータス列が空白になること
  - 絞り込みフィルターで「いずれかの区分が該当ステータス」の顧問先が表示されること
  - 処遇改善加算・ベースアップ評価料タブの既存動作（1顧問先1ステータス）に影響がないこと
  - 全タブのステータス選択肢・絞り込みフィルターに「自社作成」が追加されていること
- 臨時の補助金の補助金名称機能（2026-08-21追加）も実ブラウザでの動作確認が未実施。次回ログイン時に以下を確認する必要あり：
  - 臨時の補助金タブの計画書・実績報告サブタブ行の右側に名称欄が表示されること
  - クリックで編集でき、Enter・フォーカスアウトで保存されること
  - 年度を切り替えると対応する年度の名称に表示が切り替わること
  - 計画書・実績報告で同じ名称が共有されること、再読み込み後も保持されること
  - 処遇改善加算・ベースアップ評価料タブの表示に影響がないこと

## 2026年8月3日 実施内容
- 臨時の補助金 実績報告ページのボタン変更
  - 「処遇改善加算からコピー」「ベースアップ評価料からコピー」を削除
  - 「計画書からコピー」ボタンを追加
- 全タブの絞り込みフィルター修正
  - 「年度（すべて）」フィルターを削除
  - 「納品日（すべて）」→「ステータス（すべて）」に変更
  - ステータス選択肢：すべて・未着手・依頼中・収集済・集計完了・納品済
  - ステータス選択肢のセレクトボックス幅を修正

## NEXT_ACTION
- 次回作業開始時に `git fetch origin`、指示文書、Git状態を再確認する
- Firebase関連の変更時は、実データ確認と影響範囲整理を先に行う
- 次回ログイン時に、2026-08-21の変更（臨時の補助金の区分ごとステータス・全タブ「自社作成」追加・補助金名称表示編集機能）を実ブラウザで確認する（上記「既知の課題」参照）

## 最終更新
- 最終更新AI: Claude Code
- 最終更新日時: 2026-08-21（日本時間）
- 変更内容: 臨時の補助金タブに年度ごとの補助金名称表示・編集機能を追加（Firebaseパス `shogu/rinjiSubsidyNames/{年度}` 新設）。コミット`e47e04e`をpush。実ブラウザでの動作確認は利用者の判断により未実施。

### 過去の更新

- 2026-08-03（Claude Code）: 臨時の補助金・実績報告ページのボタン変更と全タブの絞り込みフィルター修正。詳細は「直近の変更」参照。最終更新日時: 2026-08-03（日本時間）
